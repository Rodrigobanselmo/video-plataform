
---->Icons
import {Icons} from '../../Icons/iconsDashboard'

---->constants
import {lists} from '../../../constants/itemsDrawer'

---->useTheme
import { useTheme } from '@material-ui/core/styles';

---->loaderDash
import {useLoaderScreen} from '../../../context/LoaderContext'
const {load,setLoad} = useLoaderScreen();

---->Notifications

---->hooks (wait action)
import useTimeOut from '../../../hooks/useFadeInOut'
const [noHandle,newValue,onActionTimeOut] = useFadeInOut();



---->hooks (clean/timeOut)
import useTimeOut from '../../../hooks/useTimeOut'
const [onTimeOut,onClearTimeOut] = useTimeOut();

---->hooks (clean/timeOut)

---->hooks (clean/timeOut)

---->REDUCER
import { useSelector,useDispatch } from 'react-redux'
const activeRoute = useSelector(state => state.route)
const dispatch = useDispatch()