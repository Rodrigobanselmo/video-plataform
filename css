&:not(:last-child) {}

&:hover ${Button} {} //afeta outro elemento no hover

&:hover ~ ${ContentB} {} // todos depois do item que forem COMTENTB
> somente childs diretos do elemento
+ somente o item seguinte a o elemento
